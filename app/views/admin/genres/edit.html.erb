<div class="flash">
  <%= flash[:notice] %>
</div>
<h1>ジャンル編集</h1>

<%= form_with model: @genre, url: admin_genre_path(@genre.id), method: :patch do |f| %>

<div>
  ジャンル名  <%= f.text_field :name %> <%= f.submit '変更を保存' %>
</div>
<% end %>