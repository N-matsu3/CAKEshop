<div class="container">
  <div class="row">
    <div class="col mt-5 ml-5">
      <h2 class="col ml-5 pb-3 bg-secondary-subtle">新規会員登録</h2>

        <table class="table table-borderless">
        <%= form_with model:@customer, url: customer_registration_path do |f| %>
          <%= render "devise/shared/error_messages", resource: resource %>

          <tr>
            <th>名前</th>
            <td><%= f.label :（姓）　 %>
              <%= f.text_field :last_name, autofocus: true, autocomplete: "last_name" %></td>
            <td><%= f.label :（名）　 %>
              <%= f.text_field :first_name, autofocus: true, autocomplete: "first_name" %></td>
          </tr>

          <tr>
            <th>フリガナ</th>
            <td><%= f.label :（セイ） %>
            <%= f.text_field :last_name_kana, autofocus: true, autocomplete: "last_name_kana" %>

            <td><%= f.label :（メイ） %>
            <%= f.text_field :first_name_kana, autofocus: true, autocomplete: "first_name_kana" %></td>
          </tr>


          <tr>
            <th>メールアドレス</th>
            <%#= f.label :メールアドレス %>
            <td>　　　　 <%= f.email_field :email, autofocus: true, autocomplete: "email" %></td>
          </tr>

          <tr>
            <th>郵便番号（ハイフンなし）</th>
            <%#= f.label :%>
            <td>　　　　 <%= f.text_field :postal_code, autofocus: true, autocomplete: "postal_code" %></td>
          </tr>

          <tr>
            <th>住所</th>
            <td>　　　　 <%= f.text_field :address, autofocus: true, autocomplete: "address" %>

          <tr>
            <th>電話番号（ハイフンなし）</th>
            <td>　　　　 <%= f.text_field :telephone_number, autofocus: true, autocomplete: "telephone_number" %>

          <tr>
            <th>パスワード
              <% if @minimum_password_length %>
              <em>(<%= @minimum_password_length %> 文字以上)</em>
              <% end %>
            </th>
            <td>　　　　 <%= f.password_field :password, autocomplete: "new-password" %></td>
          </tr>

          <tr>
            <th>パスワード（確認用）</th>
            <td>　　　　 <%= f.password_field :password_confirmation, autocomplete: "new-password" %></td>
          </tr>

          <tr>
            <th></th>
            <td>　　　　 <%= f.submit "新規登録" ,class: "btn btn-success w-50"%></td>
          </tr>

        <% end %>
        </table>

        <div class="p-3 mb-2 bg-secondary-subtle text-dark">既に登録済みの方</div>
         <%= link_to 'こちら', new_customer_session_path %>からログインしてください。
  </div>
</div>