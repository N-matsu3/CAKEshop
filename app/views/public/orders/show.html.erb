<h4>注文履歴詳細</h4>

<div>
 <h5>注文情報</h5>
 <table>
  <tr><th>注文日</th>　<td><%= @order.created_at.strftime('%Y/%m/%d') %></td></tr>
  <tr><th>配送先</th><td><%= @order.post_code %></br>
                         <%= @order.shipping_address %></br>
                         <%= @order.shipping_name %></td></tr>
  <tr><th>支払方法</th><td><%= @order.payment_method %></td></tr>
 </table>
</div>

<div>
 <h5>請求情報</h5>
 <table>
  <tr><th>商品合計</th>　<td><%= @order.total_payment %></td></tr>
  <tr><th>配送料</th><td><%= @order.postage %></td></tr>
  <tr><th>ご請求額</th><td><%= @order.total_payment + @order.postage %></td></tr>
 </table>
</div>

<div>
 <h5>注文内容</h5>
<table>
 <thead>
  <tr>
   <th>商品</th>
   <th>単価（税込）</th>
   <th>個数</th>
   <th>小計</th>
  </tr>
 </thead>
 <tbody>
<% @order.order_details.each do |order_detail| %>
  <tr>
   <td><%= order_detail.item.name %></td>
   <td><%= order_detail.price %></td>
   <td><%= order_detail.quantity %></td>

   <td><%= order_detail.price * order_detail.quantity %></td>
  </tr>
<% end %>
 </tbody>
</table>
</div>